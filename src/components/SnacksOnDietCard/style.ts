import styled from 'styled-components/native'

export const SnacksOnDietContainer = styled.View`
  justify-content: center;
  align-items: center;
  padding: 16px;
  gap: 8px;
  border-radius: 8px;

  width: 157.5px;
  height: 107px;
  background-color: ${({ theme }) => theme.COLORS.GREEN_LIGHT};
`
